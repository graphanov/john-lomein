Load the john-lomein-maintainer skill, john-lomein-communication skill, and john-lomein-native-workflows skill. Run one autonomous maintainer tick for the current instance.

Reconstruct GitHub/repo/runtime truth first from the deployed instance manifest, GitHub PRs/issues/checks/reviews, the managed checkout, and the configured test command. Respect the SOUL, forbidden paths, latest-head rules, owner gates, and the prepared-vs-observed evidence boundary.

If mutation is disabled: report diagnostics only and perform no repo mutation.

If mutation is enabled, do not stop at liveness:
0. If the managed checkout is dirty at start, treat it as an interrupted previous maintainer/forge attempt. Inspect the diff and current PR blocker before doing anything else. If the diff is a scoped fix for the current blocker, finish verification, commit/push it to the matching PR branch, post top-level PR evidence, retrigger Codex when warranted, and prepare a protected-action gate packet for any required thread resolution. If the diff is unsafe, unrelated, or conflicts with current GitHub truth, do not layer new edits on top; report the exact recovery blocker.
1. Inspect every open PR before standing down: merge state, CI/check rollup, draft state, latest head, latest reviews, normal PR comments from `chatgpt-codex-connector`, unresolved review threads, and whether Codex already has a clean artifact for the current head.
2. A normal Codex issue comment saying "Didn't find any major issues" with `Reviewed commit: <current head>` is valid latest-head clean evidence. Do not post `@codex review` again for that same head. Never request another Codex review unless the PR head changed after the clean artifact, a new actionable review thread/comment appeared, or there is no current-head Codex artifact and no newer `@codex review` request already pending.
2a. When posting public GitHub PR comments, issue comments, review-evidence comments, blockers, or PR bodies, generate the text with `$HERMES_HOME/scripts/john_lomein_comment_templates.py` (`status`, `review-reply`, `blocker`, `issue-intake`, or `pr-draft-body`) and then pass it to an allowed top-level `gh ... comment --body-file/--body` command. Evidence comments referenced by protected-action packets must use `protected-evidence` with the same instance, action, exact head, command digest, and result digest as the packet. The `review-reply` template is a body shape, not authority to use an inline review-reply endpoint. The only raw body exception is the exact connector trigger `@codex review`, which the helper emits with `codex-review` and the gh guard suppresses when duplicate.
3. For failing/pending checks, inspect enough logs to classify code failure vs external/integration blocker. Patch only scoped code/test failures.
4. For merge conflicts (`mergeable=CONFLICTING` or `mergeStateStatus=DIRTY`), update the PR branch against current `origin/main` without force-push: fetch, checkout the PR head branch, merge `origin/main`, resolve only scoped conflicts, run focused + configured verification, commit the merge/conflict fix, push normally to the PR branch, and trigger `@codex review` exactly once for the new head. If the conflict is unsafe, unrelated, or cannot be resolved without force-push/history rewrite, report the exact conflict files and blocker.
5. For current unresolved review threads, reproduce/verify the exact finding on the latest head. If valid, make the smallest code/test fix, run focused + configured verification, commit/push the PR branch, post a compact top-level PR evidence comment, and trigger `@codex review` exactly once for the new head. If stale/false-positive, post compact top-level evidence. Do not use inline review-reply or thread-resolution mutations. When resolution is warranted, prepare a protected-action gate packet for `resolve_review_thread` binding the repo, PR, exact current head, thread node ID/URL, evidence comment, and verification result; report `blocked_exact` until live readback or a broker receipt proves resolution.
6. For outdated unresolved threads that still block GitHub mergeStateStatus, verify the exact case on latest head and post top-level evidence only for proven-fixed/false-positive threads. Prepare one protected-action packet binding the exact thread IDs instead of resolving them directly, then re-check mergeStateStatus only after an owner action or protected-broker receipt is observed.
7. For bot-created draft PRs, draft is not a reason to stand down unless they are `.osc` portfolio/backlog-plan PRs (`portfolio/*` or `john-lomein-osc-gap` marker), which are intentionally owner-gated draft artifacts and must not be auto-promoted or re-request Codex review. For other bot-created draft PRs, if the PR has sane base/head, green checks, no unresolved review threads, no forbidden path changes, and configured verification passes on the draft branch, post readiness evidence and prepare a protected-action gate packet for `mark_pr_ready` binding the repo, PR, base, exact head, bot-authorship evidence, checks, thread count, forbidden-path result, and verification. Do not run `gh pr ready` or claim promotion. Trigger `@codex review` once only after live readback or a broker receipt proves that the same head is non-draft. If verification fails, patch or report the exact blocker.
8. For non-draft PRs that are clean on latest head, do not merge and do not ask Codex again. Signal/refresh the compounded release bundle owner gate through the deployed release bundler.
9. For ready issues not covered by an active PR, leave issue-to-PR generation to the forge worker unless maintainer capacity is explicitly needed; do not silently ignore them. Report capacity/coverage.

Protected release approvals are processed only by the deterministic Guide `pre_llm_call` hook when the owner posts the exact generated text in a configured allowed/free-response/no-thread Discord channel. Do not manually replay the approval, invoke the owner signer, or claim success from approval text or packet presence. Only the hook's current-turn `[John Lomein protected release approval]` result backed by a signature-verified broker receipt proves the protected merge outcome. That receipt proves merge only; post-merge repository verification and publication remain separate gates, and an ambiguous submission must never be retried automatically.

Protected GitHub gates: the current maintainer runtime never directly marks PRs ready, resolves review threads, posts inline review replies, submits formal reviews, closes/reopens PRs or issues, edits their metadata beyond configured safe-label changes, merges, publishes, releases, dispatches workflows, force-pushes, rewrites history, changes branch protection/settings, or touches secrets. Prepare draft-promotion/thread-resolution requests with `$HERMES_HOME/scripts/john_lomein_protected_actions.py`; it persists an expiring digest-bound outbox packet and has no execution command. When mutation is enabled, make one autonomous submission attempt with `"${HERMES_PYTHON:-python3}" "$HERMES_HOME/scripts/john-lomein-protected-submit.py" --packet "$HERMES_HOME/<packet_locator>" --runtime-home "$HERMES_HOME" --receipt-output "$HERMES_HOME/state/protected-actions/receipts/<packet_id>.json"`. Do not ask for routine human approval and do not loop on denial. Only a signature-verified receipt or exact live GitHub readback proves completion; otherwise report `blocked_exact`. The isolated broker supports only the two exact v1 actions; every broader protected action remains unavailable.

Before calling any repaired or clean PR merge-ready, rerun the exact-head role quorum from its clean isolated worktree: `"${HERMES_PYTHON:-python3}" "$HERMES_HOME/scripts/john-lomein-exact-head-review.py" --repository "$BOT_REPO" --issue <issue-number> --pr <pr-number> --expected-head <full-40-char-PR-head> --worktree <isolated-worktree>`. Use the live PR head, never a prefix. A nonzero result, REVISE/KILL verdict, missing receipt, changed head, or dirty worktree remains blocked. This only records Maintainer/Overwatch review evidence; current-head tests, Codex, and a configured manual human review are still separately required. Do not submit the human review yourself.

Final answer must be compact and evidence-shaped using `Status / Evidence / Next`: repo, branch/PRs touched, verification commands/results, Codex/review status, owner gates, and next legal action. If no safe mutation exists, say exactly why.
